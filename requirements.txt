# Claude-Data-Wrangler — aggregated dependencies for all skills.
# Install everything with: ./scripts/install-deps.sh
# Or cherry-pick per skill; each SKILL.md lists its minimum requirements.

# Core: tabular I/O
pandas>=2.0
pyarrow>=14
openpyxl>=3.1

# ISO / locale / language
pycountry>=23
babel>=2.13
langcodes>=3.4
iso4217>=1.11
python-stdnum>=1.19

# Dates
python-dateutil>=2.8

# Encoding / text
charset-normalizer>=3.3
chardet>=5.2
ftfy>=6.2
confusable-homoglyphs>=3.3

# PII detection / anonymisation
phonenumbers>=8.13
presidio-analyzer>=2.2
presidio-anonymizer>=2.2
faker>=24

# Enrichment
holidays>=0.47

# SQL
SQLAlchemy>=2.0
psycopg[binary]>=3.1
PyMySQL>=1.1
pyodbc>=5.1
duckdb>=0.10

# Vector
sentence-transformers>=2.7
pinecone-client>=3.2
qdrant-client>=1.9
weaviate-client>=4.5
pymilvus>=2.4
chromadb>=0.5

# Graph
neo4j>=5.18
python-arango>=7.9
gqlalchemy>=1.5

# API / OpenAPI / MCP
httpx>=0.27
tenacity>=8.2
prance>=23.6
openapi-spec-validator>=0.7

# HF Hub
huggingface_hub>=0.24

# YAML / misc
PyYAML>=6.0
zstandard>=0.22
